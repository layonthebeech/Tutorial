function repeat(operation, num) {
  return operation.call(num);//call the function operation num times by taking in num as a parameter 
  //
}

// Do not remove the line below
module.exports = repeat;

